apiVersion: apps/v1
kind: Deployment
metadata:
  name: sched-builder-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend-container
        image: jjtschooldlsud/sched-builder-db:v1.4
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3000
        env:
        - name: DB_USER
          valueFrom:
            configMapKeyRef:
              name: sched-builder-config
              key: DB_USER
        - name: DB_HOST
          valueFrom:
            configMapKeyRef:
              name: sched-builder-config
              key: DB_HOST
        - name: DB_NAME
          valueFrom:
            configMapKeyRef:
              name: sched-builder-config
              key: DB_NAME
        - name: DB_PORT
          valueFrom:
            configMapKeyRef:
              name: sched-builder-config
              key: DB_PORT
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: sched-builder-config
              key: PORT
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: sched-builder-secret
              key: DB_PASSWORD
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: sched-builder-secret
              key: JWT_SECRET