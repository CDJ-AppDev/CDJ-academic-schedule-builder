apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-deployment-sched-builder
spec:
  replicas: 10
  selector:
    matchLabels:
      app: web-site
  template:
    metadata:
      labels:
        app: web-site
    spec:
      containers:
      - name: web-container
        image: schedule-builder:v1
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80